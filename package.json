{
  "name": "zalohub-platform",
  "version": "5.0.6",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build:foundation": "npm run build --workspace=@zalohub/contracts && npm run build --workspace=@zalohub/management-api && npm run build --workspace=@zalohub/zalo-gateway && npm run build --workspace=@zalohub/web-platform",
    "start:zalo-gateway": "npm run start --workspace=@zalohub/zalo-gateway",
    "start:management-api": "npm run start --workspace=@zalohub/management-api",
    "start:web-platform": "npm run start --workspace=@zalohub/web-platform"
  },
  "engines": {
    "node": ">=22"
  }
}
